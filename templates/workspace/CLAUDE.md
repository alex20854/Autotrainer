# Autotrainer Workspace

This is an athlete's training workspace for the Autotrainer cardio coach. Use
the `/coach` skill (`setup | ingest | plan | review | ask`); its
`workspace.md` holds the file map and standing rules. The engine (scripts,
knowledge base, schema) lives in a separate repo — never copy engine code or
general knowledge in here, and never write athlete data into the engine.

## Workspace notes

- Engine: `<path to Autotrainer checkout>`
- Hosted dashboard: _(none yet — recorded by the first `/coach ingest` that publishes it)_
- Repo visibility: _(public | private)_
